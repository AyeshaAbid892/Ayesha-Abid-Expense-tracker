# Local development: leave unset, it falls back to http://localhost:4000/api
# Production (Vercel): set this to your deployed Render backend URL, e.g.
# VITE_API_BASE_URL=https://ledgerly-api.onrender.com/api
VITE_API_BASE_URL=
